# GHG Persistence Study — Replication Code

This repository contains the analysis scripts to reproduce all results, tables, and figures in the paper.

---

## Requirements

- Python 3.10 or higher
- Install dependencies:

```
pip install -r requirements.txt
```

---

## Data

Download the raw data file from EDGAR (Emissions Database for Global Atmospheric Research):

- Source: https://edgar.jrc.ec.europa.eu/
- File: `GHG_totals_by_country.csv`
- Place the file in the root of the `GHG_Trajectory_Analysis_Study/` folder

---

## Project Structure

```
GHG_Trajectory_Analysis_Study/
├── GHG_totals_by_country.csv       # Place raw EDGAR data here
├── data/
│   └── processed/                  # Generated by preprocessing.py
├── outputs/
│   ├── figures/                    # Generated figures
│   └── tables/                     # Generated tables
├── scripts/
│   ├── preprocessing.py
│   ├── Analysis_Trajectory Classification.py
│   ├── Figure2_Timeseries.py
│   ├── Figure3_PostParis_CAGR.py
│   ├── Sensitivity Analysis_Sample Composition.py
│   └── Sensitivity Analysis_Threshold.py
├── requirements.txt
└── README.md
```

---

## How to Run

Scripts must be run **in the following order**. Each script depends on the output of the previous one.

### Step 1 — Data Preprocessing

```
python scripts/preprocessing.py
```

Reads the raw EDGAR CSV, cleans the data, and saves the analysis-ready panel dataset.

Output:
- `data/processed/Processed_GHG_totals_by_country.csv`
- `outputs/tables/summary_statistics.xlsx`
- `outputs/tables/preprocessing_report.txt`

---

### Step 2 — Trajectory Classification

```
python scripts/"Analysis_Trajectory Classification.py"
```

Classifies each country into a trajectory category and computes key metrics.

Output:
- `outputs/tables/Analysis_Trajectory Classification.xlsx`

---

### Step 3 — Figure 2

```
python scripts/Figure2_Timeseries.py
```

Generates the 4-panel time series figure.

Output:
- `outputs/figures/Figure2_TimeSeries.png`

---

### Step 4 — Figure 3

```
python scripts/Figure3_PostParis_CAGR.py
```

Generates the post-Paris Agreement CAGR distribution figure.

Output:
- `outputs/figures/Figure3_PostParis_CAGR.png`

---

### Step 5 — Sensitivity Analysis I (Threshold)

```
python scripts/"Sensitivity Analysis_Threshold.py"
```

Tests robustness of results to alternative classification threshold specifications.

Output:
- `outputs/tables/Sensitivity Analysis_Threshold.xlsx`

---

### Step 6 — Sensitivity Analysis II (Sample Composition)

```
python scripts/"Sensitivity Analysis_Sample Composition.py"
```

Tests robustness of results to exclusion of small-emitting countries.

Output:
- `outputs/tables/Sensitivity Analysis_Sample Composition.xlsx`

---

## Notes

- All scripts automatically resolve paths relative to the script location. They can be run from any working directory.
- The `outputs/` directories are created automatically if they do not exist.
